import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  sendButton: {
    width: '100%',
    backgroundColor: '#2573DA',
    padding: 10,
    marginTop: 15,
    borderRadius: 5,
    alignItems: 'center',
  },
  sendButtonText: {
    color: '#fff',
    fontWeight: 'bold',
  },
});
